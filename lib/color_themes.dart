import 'package:flutter/material.dart';

class ColorThemes {
  Color primaryColor = Colors.blueAccent;
  Color buttonColor = Colors.blueAccent;
  Color updateButtonColor = Color.fromARGB(255, 28, 160, 61);
  Color cancelButtonColor = Color.fromARGB(255, 102, 102, 102);
}

ColorThemes color = new ColorThemes();
